HealthCompass
This project aims to develop an efficient system for locating the most suitable nearby hospitals based on user’s current location. The core of the system is built using Vantage Point Trees (VP Trees), which enables fast nearest neighbour and ranged based searches in a spatial dataset. By representing hospitals as points in a metric space, using geographic coordinates, the system can efficiently find hospitals in a specified radius and rank them based on proximity.
